{
  "name": "sshunifi",
  "version": "1.0.0",
  "description": "Local SSH tool to connect to unifi usg",
  "main": "./main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager ./ unifilocal --platform=darwin --out ./dist --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "rebuild" : "electron-rebuild -f -w simple-ssh ping keytar"
  },
  "author": "Richard Styles",
  "license": "TBD",
  "dependencies": {
    "electron-prebuilt": "^1.4.3",
    "bootstrap": "3",
    "bootstrap-sass": "^3.3.7",
    "jquery": "^3.1.1",
    "keytar": "^3.0.2",
    "ping": "^0.2.0",
    "simple-ssh": "^0.9.0"
  },
  "devDependencies": {
    "electron-packager": "^8.0.0",
    "electron-rebuild": "^1.2.1",
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.0",
    "gulp-jshint": "^2.0.1",
    "gulp-rename": "^1.2.2",
    "gulp-sass": "^2.3.2",
    "gulp-uglify": "^2.0.0",
    "jshint": "^2.9.3"
  }
}
